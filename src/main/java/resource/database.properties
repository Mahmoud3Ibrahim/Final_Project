# =============================================================================
# PTFMS Database Configuration Properties
# Public Transit Fleet Management System
# CST8288 Final Project
# =============================================================================

# Database Connection Settings
db.driver=com.mysql.cj.jdbc.Driver
db.url=jdbc:mysql://localhost:3306/ptfms_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8
db.username=cst8288
db.password=cst8288

# Connection Pool Settings
db.initialPoolSize=5
db.maxPoolSize=20
db.minPoolSize=2
db.acquireIncrement=2
db.maxStatements=50
db.maxIdleTime=300

# Database Schema Settings
db.schema=ptfms_db
db.autoReconnect=true
db.useSSL=false

# Application Settings
app.name=Public Transit Fleet Management System
app.version=1.0.0
app.environment=development

# Session Settings
session.timeout=30
session.secure=false

# File Upload Settings
upload.maxFileSize=10485760
upload.maxRequestSize=52428800
upload.tempDirectory=/tmp

# Logging Settings
log.level=DEBUG
log.file.path=logs/ptfms.log
log.file.maxSize=10MB
log.file.maxHistory=30

# Email Settings (for alerts)
email.smtp.host=smtp.gmail.com
email.smtp.port=587
email.smtp.auth=true
email.smtp.starttls=true
email.username=ptfms@example.com
email.password=app_password

# Maintenance Alert Settings
maintenance.alert.threshold.brake=2000
maintenance.alert.threshold.tire=15000
maintenance.alert.threshold.engine=5000
maintenance.alert.threshold.electrical=8000

# Fuel Efficiency Thresholds
fuel.efficiency.bus.min=20.0
fuel.efficiency.rail.min=12.0
fuel.efficiency.train.min=35.0